npm create vite@latest prueba --template react
(Ir a la carpeta)
npm i

npm run build   Para generar carpeta desplegable
Para que haga rutas relativas funcionales con hosts ftp en el package.json se pone el atributo:
"homepage": "./",
